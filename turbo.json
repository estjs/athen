{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", "vendors/**"],
      "env": ["NODE_ENV"]
    },
    "dev": {
      "cache": false
    },
    "//#docs:build": {
      "dependsOn": ["^build"],
      "outputs": ["docs/dist/**"]
    }
  }
}
