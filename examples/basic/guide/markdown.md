# Markdown Configuration

## Rendered Headings

This heading appears in the page outline because `markdown.toc.level` includes level 2.

### Nested Heading

This level 3 heading also appears in the outline.

#### Ignored Heading

This level 4 heading is intentionally outside the configured TOC range.

## Code Blocks

```ts
const framework = 'athen';
const feature = 'markdown config';
console.log(`${framework}: ${feature}`);
```

## External Links

[Athen repository](https://github.com/estjs/athen) opens in a new tab.
