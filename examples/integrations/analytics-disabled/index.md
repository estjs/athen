# Analytics Disabled Preset

This preset uses `analytics: false`, so the built-in analytics plugin does not inject provider scripts.

```ts
export default defineConfig({
  title: 'Analytics Disabled Preset',
  analytics: false,
});
```
