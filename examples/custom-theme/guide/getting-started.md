# Custom Theme Guide

The custom theme uses Athen runtime data and renders child routes with `RouterView`.

## What it shows

- A local `theme` directory.
- A custom layout component.
- Default markdown styles reused inside a custom shell.
