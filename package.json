{
  "name": "athen-monorepo",
  "version": "0.0.8-beta.10",
  "packageManager": "pnpm@11.7.0",
  "type": "module",
  "license": "MIT",
  "homepage": "https://github.com/estjs/athen#readme",
  "bugs": "https://github.com/estjs/athen/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/estjs/athen.git"
  },
  "author": "jiangxd <jiangxd2016@gmail.com>",
  "scripts": {
    "build": "turbo run build --filter=./packages/*",
    "dev": "turbo watch build --filter=./packages/*",
    "docs:dev": "athen dev docs",
    "docs:build": "athen build docs",
    "docs:preview": "athen preview docs",
    "lint": "eslint --cache --fix \"./**/src/*.{ts,tsx,js,jsx}\"",
    "test": "vitest",
    "test:e2e": "playwright test",
    "coverage": "vitest run --coverage",
    "test:watch": "vitest --watch",
    "typecheck": "turbo run typecheck",
    "changelog": "tsx scripts/changelog.ts",
    "tempversion": "tsx scripts/temp-version.ts",
    "prerelease": "pnpm run build && pnpm run lint && pnpm run typecheck && pnpm run test -- --run",
    "release": "pnpm run prerelease && pnpm run changelog && bumpp package.json packages/*/package.json --no-git-checks --all --execute \"pnpm run tempversion\""
  },
  "devDependencies": {
    "@estjs/eslint-config": "^2.3.0",
    "@playwright/test": "^1.60.0",
    "@vitest/coverage-v8": "^4.1.9",
    "athen": "workspace:*",
    "bumpp": "^11.1.0",
    "eslint": "^10.5.0",
    "lint-staged": "^17.0.7",
    "picocolors": "^1.1.1",
    "pnpm": "^11.7.0",
    "prettier": "^3.8.4",
    "simple-git-hooks": "^2.13.1",
    "standard-changelog": "7.0.1",
    "tsx": "^4.22.4",
    "turbo": "^2.9.18",
    "typescript": "^6.0.3",
    "vitest": "^4.1.9"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm exec lint-staged --concurrent false",
    "commit-msg": "pnpm exec tsx ./scripts/verify-commit.ts $1"
  },
  "lint-staged": {
    "packages/*/src/**/*.{ts,tsx,js}": "eslint --cache --fix"
  }
}
